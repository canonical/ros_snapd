name: Monthly
on:
  schedule:
    - cron: '0 0 1 * *'
  workflow_dispatch:

jobs:
  noetic:
    uses: canonical/ros_snapd/.github/workflows/snap.yaml@noetic
  humble:
    uses: canonical/ros_snapd/.github/workflows/snap.yaml@humble
  jazzy:
    uses: canonical/ros_snapd/.github/workflows/snap.yaml@jazzy
