name: Monthly
# on:
#   schedule:
#     - cron: '0 0 1 * *'
#   workflow_dispatch:
on:
  schedule:
    - cron: '00 14 * * *'
  workflow_dispatch:

jobs:
  noetic:
    uses: canonical/ros_snapd/.github/workflows/snap.yaml@fix/monthly
    with:
      git-ref: "noetic"
  humble:
    uses: canonical/ros_snapd/.github/workflows/snap.yaml@fix/monthly
    with:
      git-ref: "humble"
  jazzy:
    uses: canonical/ros_snapd/.github/workflows/snap.yaml@fix/monthly
    with:
      git-ref: "jazzy"
